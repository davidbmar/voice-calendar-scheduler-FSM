# ── LLM ──────────────────────────────────────
LLM_PROVIDER=claude                    # claude | ollama
ANTHROPIC_API_KEY=sk-ant-...
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_URL=http://localhost:11434

# ── Twilio (PSTN + TURN) ────────────────────
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=+1...

# ── Google Calendar (service account) ────────
GOOGLE_SERVICE_ACCOUNT_JSON=path/to/service-account.json
GOOGLE_CALENDAR_ID=primary

# ── RAG Service ──────────────────────────────
RAG_SERVICE_URL=http://localhost:9900

# ── Admin Auth ───────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# When set, all admin/mutating APIs require Authorization: Bearer <key>
# When empty + DEBUG=true: admin APIs are open (local dev)
# When empty + DEBUG=false: admin APIs are locked (production safety)
ADMIN_API_KEY=

# ── Server ───────────────────────────────────
HOST=127.0.0.1
PORT=8090
DEBUG=false

# ── WebRTC fallback ICE servers ──────────────
ICE_SERVERS_JSON=[{"urls":"stun:stun.l.google.com:19302"}]
